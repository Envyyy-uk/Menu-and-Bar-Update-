# Спринти

Повний план лежить у `docs/PROJECT_PLAN.md`. Тут — розбивка того плану на
короткі спринти (сесії), кожен зі своїм критерієм готовності. Правило з плану
лишається: **не починати наступний спринт, поки попередній не проходить свій
критерій**.

Після кожного спринту в `docs/FEEDBACK.md` дописується мінімальний звіт:
що зроблено, що змінено. Гілка одна — `main`.

| # | Спринт | Фаза плану | Критерій готовності | Стан |
|---|---|---|---|---|
| 0 | Каркас репозиторію | — | репо має план, розбивку, демо-дані, `.gitignore` | ✅ |
| 1 | Бекенд-каркас | Фаза 1 | `docker compose up` піднімає все з нуля; `GET /api/menu` віддає демо-меню | ✅ |
| 2 | Гостьове меню | Фаза 2 | гість знаходить страву за інгредієнтом будь-якою з 6 мов і фільтрує за алергенами | ✅ |
| 3 | Автентифікація й ролі | Фаза 3 | тести підтверджують, що `staff` отримує 403 на цінах, поверненнях і створенні акаунтів | ✅ |
| 4 | Адмін-панель | Фаза 4 | менеджер вимикає позицію з телефона — гість бачить це без перезавантаження | — |
| 5 | Замовлення без оплат | Фаза 5 | подвійний тап не створює два замовлення, обрив мережі не втрачає жодного | — |
| 6 | Stripe Connect | Фаза 6 | `paid` виставляється виключно вебхуком — тест із «закритим браузером» це підтверджує | — |
| 7 | Екран кухні | Фаза 7 | втрата зв'язку дає гучну помилку, а не тихий застарілий список | — |
| 8 | Фейковий сервіс | Фаза 8 | є сценарій прогону й чекліст, збої йдуть в issues | — |

---

## Спринт 0 — каркас репозиторію

Що входить: `README.md`, ця розбивка, `docs/PROJECT_PLAN.md`, `docs/FEEDBACK.md`,
`.gitignore`, `seed_menu.json` із демонстраційними даними розділу 14.

Готово, коли: у репозиторії є план, розбивка й демо-дані, і все це на `main`.

## Спринт 1 — бекенд-каркас

Docker Compose (FastAPI + Postgres), Alembic, схема з розділу 6 плану,
health-check, сідер із `seed_menu.json`, `GET /api/menu`.

Готово, коли: `docker compose up` піднімає все з нуля на чистій машині,
`GET /health` віддає `ok`, `GET /api/menu` — демо-меню.

## Спринт 2 — гостьове меню

Перенесена з референсу інфраструктура: словник інгредієнтів, 14 алергенів,
i18n на 6 мов, теми, PWA, розклади. Рендеринг з API, не зі статичних файлів.

Готово, коли: пошук «кунжут» / «sesame» / «Sesam» дає одні й ті самі позиції,
а фільтр алергенів приглушує те, що треба.

## Спринт 3 — автентифікація й ролі

Таблиці `users`, `devices`, `audit_log`. PIN на зареєстрованому пристрої для
`staff`, пошта й пароль для решти. Матриця прав із розділу 9, перевірка на
кожному ендпойнті. Сідер першого `owner`.

Готово, коли: тести дають `staff` 403 на цінах, поверненнях і створенні акаунтів.

## Спринт 4 — адмін-панель

Стани позицій, розклади, ціни, `station`, столи з QR і ротацією токенів,
живий список замовлень, аудит-лог. Механізму публікації немає — запис одразу в БД.

Готово, коли: зміна стану позиції в панелі доходить до гостя без перезавантаження.

## Спринт 5 — замовлення без оплат

Токени столів, `/t/{token}`, кошик, машина станів розділу 7, ідемпотентність за
`client_token`, розділення на `kitchen` і `bar`.

Готово, коли: подвійний POST з тим самим `client_token` повертає те саме замовлення.

## Спринт 6 — Stripe Connect

Direct charges на акаунті закладу, `application_fee_amount`, Checkout,
ідемпотентні вебхуки за `stripe_event_id`, повернення з лімітом для `manager`,
звірка `paid` → `accepted`.

Готово, коли: `paid` виставляється лише з вебхука; тест це доводить.

## Спринт 7 — екран кухні

WebSocket + fallback на polling, індикатор зв'язку, звук, кнопки «Прийнято» /
«Готово», повне перезавантаження стану після відновлення зв'язку.

Готово, коли: тиша від сервера понад 10 секунд дає червоний банер і звук.

## Спринт 8 — фейковий сервіс

Сценарій прогону на шістьох знайомих, чекліст спостережень, скрипт навантаження
(чотири замовлення в одну хвилину), шаблон issue для збоїв.

Готово, коли: прогін можна провести за документом, нічого не питаючи.
